pipeline {
    agent any
    stages {
        stage ('This is First stage') {
            def course = 'K8S'
            if (course == "${K8S}")
            println ('welcome to ${K8S} - Course')
            else
            println ('Please enrool to ${K8S} - Course ')
        }
    }
}
